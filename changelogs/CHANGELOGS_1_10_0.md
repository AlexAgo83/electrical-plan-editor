# Changelog (`1.9.81 -> 1.10.0`)

## Version 1.10.0 - Release Version Realignment

### Release

- Aligned release metadata to `1.10.0` across `VERSION`, `package.json`, `package-lock.json`, and README.
- Moved the release line from `1.9.81` to `1.10.0` so the next release remains ordered correctly under semantic versioning.

## Validation and Regression Evidence

- Verified root package metadata, lockfile metadata, and `VERSION` all resolve to `1.10.0`.
